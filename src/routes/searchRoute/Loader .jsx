const Loader = () => (
      <div className="flex justify-center items-center min-h-screen">
        <div className="loader"></div> {/* You can use CSS or an external library for the loader */}
      </div>
    );
    export default Loader;
    